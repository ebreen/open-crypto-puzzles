# Open leads, ranked

## 1. Re-read piece 1 as something other than colour names or hex (hours)

Colour names, hex, colour initials, `000111`, and short IQ tokens (`Blue`, `White`,
`Navy`, `Cyan`, `Azure`, `RGB`, `CMYK`, `Primary`, `Additive`, `Missing`, `Indigo`)
have all missed, including the complementary-length 2-3 atom investor concatenations
those short tokens force (row 11, 164,784 candidates). What is left is an encoding of
the flag attributes (ball, side, height, tilt) that is not one of those strings, or a
single token not on that list. Semaphore remains untested as a letter system.
Confirms: an oracle MATCH. Kills: a piece-1 rule as tight as `2111011` that still
produces no 58-character hit.

## 2. Piece 2 is still a proper noun, but not the a16z / Blue Whale Capital list (hours)

The whale plus 16-03-2020 still points at the Forbes Arweave article of that date and
at a large investor, and the cartoon is a baleen / blue whale. What has been killed
is the specific tokens `AndreessenHorowitz`, `a16z`, `16-03-2020`, `a16zcrypto`,
`ndreessenHorowit`, `BlueWhaleCapital`, `BalaenopteraMusculus`, `HargreavesLansdown`,
and the 2-3 atom concatenations of those investors with dates and Forbes headline
tokens, in every 24-order assembly with the piece-1 families above. A string that
only works once piece 1's length is revised remains open. Confirms: an oracle MATCH.
Kills: exhausting the next piece-1 rule's complementary lengths.

## 3. Confirm piece 4 is Alien with an oracle hit (hours)

`Alien` is still the natural anagram of the five printed letters. Visual order
(`IEANL`, `ILEAN`, `INEAL`, `AELIN`) was checked against the same piece-1 and piece-2
lists and also missed. Hatching-as-order was already refuted structurally. Piece 4
is only confirmed when some 58-character candidate that uses it matches. Kills: a
match that uses a different five-letter reading.
