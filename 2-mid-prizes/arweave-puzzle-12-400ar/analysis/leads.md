# Open leads, ranked

## 1. Re-read piece 1 as something other than colour names or hex (hours)

The 58-character budget only forces piece 2 to 18 characters if piece 1 is the six
colour names including Blue (28) and pieces 3 and 4 stay at 7 and 5. That partition,
the hex partitions that go with it (36+10, 42+4, 30+16), and Blue as a standalone
IQ-test answer paired with the short whale tokens already in the list, were all run
through the certified oracle with 0 matches. Solved siblings use a proper noun, a
notation, or a count, not the literal drawn object, so `Blue` (4 characters) would
force a 42-character whale string once piece 3 is `2111011` and piece 4 is `Alien`.
The earlier list had no 42-character tokens, so that partition was never assembled.
`tools/search_whale.py --family iq-long` fills it: short IQ tokens times 2-3 atom
investor/date/Forbes concatenations. Semaphore, a 6-letter initials word that is not
RIGGVB, or RGB decimals with a separator convention not yet tried, remain if that
family misses. Confirms: an oracle MATCH. Kills: a new piece-1 rule that is as tight
as the hexagon digit rule and still produces no 58-character hit.

## 2. Piece 2 is still a proper noun, but not the a16z / Blue Whale Capital list (hours)

The whale plus 16-03-2020 still points at the Forbes Arweave article of that date and
at a large investor, and the cartoon is a baleen / blue whale. What has been killed
is the specific tokens `AndreessenHorowitz`, `a16z`, `16-03-2020`, `a16zcrypto`,
`ndreessenHorowit`, `BlueWhaleCapital`, `BalaenopteraMusculus`, `HargreavesLansdown`,
and the other names in `tools/search_whale.py`, in every 24-order assembly with the
piece-1 families above. A headline token from that article (`LibraryOfAlexandria` was
tried at 19 characters), a different scientific name, or a string that only works
once piece 1's length is revised, remains open. Confirms: an oracle MATCH. Kills:
exhausting the next piece-1 rule's complementary lengths.

## 3. Confirm piece 4 is Alien with an oracle hit (hours)

`Alien` is still the natural anagram of the five printed letters. Visual order
(`IEANL`, `ILEAN`, `INEAL`, `AELIN`) was checked against the same piece-1 and piece-2
lists and also missed. Hatching-as-order was already refuted structurally. Piece 4
is only confirmed when some 58-character candidate that uses it matches. Kills: a
match that uses a different five-letter reading.
